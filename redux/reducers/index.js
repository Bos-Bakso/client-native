const initialState = {
    isLoading: true,
    message: null,
    items: [],
    detail: null
}


export default function bakso(state = initialState, action) {
    switch (action.type) {  
        default:
            return state
    }
}